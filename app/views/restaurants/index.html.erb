<h2>Check out all the restaurants!</h2>
<% @restaurants.each do |rest| %>
  <h3><%= link_to rest.name, restaurant_path(rest) %></h3>
  <h4><%= rest.category %></h4>
  <p><%= rest.address %></p>
  <p><%= rest.phone_number %></p>
<% end %>
